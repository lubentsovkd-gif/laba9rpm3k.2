<Application x:Class="laba9rpm3k._2.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:laba9rpm3k._2.ViewModels"
             xmlns:v="clr-namespace:laba9rpm3k._2.Views">
    <Application.Resources>

        <DataTemplate DataType="{x:Type vm:ContactsListViewModel}">
            <v:ContactsListView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:AboutViewModel}">
            <v:AboutView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:ContactsEditViewModel}">
            <v:ContactsEditView />
        </DataTemplate>

    </Application.Resources>
</Application>